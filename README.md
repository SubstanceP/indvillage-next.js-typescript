# Retirement Home Website build via Next.js and Typescript

- SSR for SEO

- Data passed into reusable props aquired from ./config/cardContent.json

## Getting Started

### Install Dependencies

`yarn` or `npm i`

### Run the development server

`npm run dev` or `yarn dev`
